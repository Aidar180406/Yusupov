# В терминале с активированным venv:
pip freeze | findstr "scikit-learn numpy pandas fastapi uvicorn pydantic joblib"

# Windows
python -m venv .venv
.venv\Scripts\activate

# Linux / macOS
python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt

python server.py

python api.py